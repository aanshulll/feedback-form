<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --accent: #ff385c;
      --bg: #f7f7f7;
      --text: #222;
      --subtext: #717171;
      --white: #fff;
      --border: #e5e5e5;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg);
      color: var(--text);
      line-height: 1.6;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 2rem;
    }

    .container {
      background-color: var(--white);
      max-width: 700px;
      width: 100%;
      padding: 3rem;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
      animation: fadeIn 0.5s ease-in-out;
    }

    .header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .header h1 {
      font-size: 1.8rem;
      font-weight: 600;
      color: var(--accent);
    }

    .header p {
      color: var(--subtext);
      font-size: 1rem;
      margin-top: 0.5rem;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .form-group label {
      display: block;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 0.9rem 1rem;
      font-size: 1rem;
      border: 1.8px solid var(--border);
      border-radius: 12px;
      transition: border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      border-color: var(--accent);
      outline: none;
      box-shadow: 0 0 0 4px rgba(255, 56, 92, 0.1);
    }

    .form-group textarea {
      resize: vertical;
      min-height: 100px;
    }

    .submit-btn {
      background-color: var(--accent);
      color: var(--white);
      font-size: 1.1rem;
      font-weight: 600;
      border: none;
      padding: 1rem;
      border-radius: 14px;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .submit-btn:hover {
      background-color: #e03150;
      transform: translateY(-2px);
      box-shadow: 0 6px 14px rgba(255, 56, 92, 0.25);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 600px) {
      .container {
        padding: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Feedback Form</h1>
      <p>Welcome, <%= user.name %>! We’d love to hear your thoughts.</p>
    </div>

    <form action="/home/feedback" method="POST">
      <div class="form-group">
        <label for="name">Name</label>
        <input type="text" id="name" name="name" value="<%= user.name %>" readonly>
      </div>

      <div class="form-group">
        <label for="rollno">Roll Number</label>
        <input type="text" id="rollno" name="rollno" value="<%= user.rollno %>" readonly>
      </div>

      <div class="form-group">
        <label for="teachingQuality">Teaching Quality</label>
        <textarea id="teachingQuality" name="teachingQuality" required placeholder="Your feedback on teaching quality (10–1000 characters)" minlength="10" maxlength="1000"></textarea>
      </div>

      <div class="form-group">
        <label for="courseContent">Course Content</label>
        <textarea id="courseContent" name="courseContent" required placeholder="Your feedback on course content (10–1000 characters)" minlength="10" maxlength="1000"></textarea>
      </div>

      <div class="form-group">
        <label for="classInteraction">Class Interaction</label>
        <textarea id="classInteraction" name="classInteraction" required placeholder="Your feedback on class interaction (10–1000 characters)" minlength="10" maxlength="1000"></textarea>
      </div>

      <div class="form-group">
        <label for="assessmentMethods">Assessment Methods</label>
        <textarea id="assessmentMethods" name="assessmentMethods" required placeholder="Your feedback on assessment methods (10–1000 characters)" minlength="10" maxlength="1000"></textarea>
      </div>

      <div class="form-group">
        <label for="overallExperience">Overall Experience</label>
        <textarea id="overallExperience" name="overallExperience" required placeholder="Your overall experience (10–1000 characters)" minlength="10" maxlength="1000"></textarea>
      </div>

      <button type="submit" class="submit-btn">Submit Feedback</button>
    </form>
  </div>
</body>
</html>
